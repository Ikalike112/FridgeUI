@model FridgeUI.Models.ViewModels.FridgeModelUpdateViewModel

@{
    ViewData["Title"] = "Update";
}

<h1>Update</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Update">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @Html.HiddenFor(fridgeModel => fridgeModel.Id)
            <div class="form-group">
                <label asp-for="FridgeModelForManipulateDto.Name" class="control-label"></label>
                <input asp-for="FridgeModelForManipulateDto.Name" class="form-control" />
                <span asp-validation-for="FridgeModelForManipulateDto.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FridgeModelForManipulateDto.Year" class="control-label"></label>
                <input asp-for="FridgeModelForManipulateDto.Year" class="form-control" />
                <span asp-validation-for="FridgeModelForManipulateDto.Year" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
